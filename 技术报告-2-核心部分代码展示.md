## 2）核心部分代码展示

DevFlow-Engine 的核心代码可以从“控制平面、执行平面、AI Agent、前端控制台”四条线理解。控制平面负责把一条研发流水线长期、可靠地跑起来；执行平面负责真正调用 Agent 和 LLM；前端控制台负责把每个阶段的中间产物展示给用户，并提供人工检查点。项目不是一个单点的 LLM Demo，而是围绕“需求分析、方案设计、代码生成、测试生成、应用并运行测试、代码评审、交付集成”搭建了一套可观察、可重试、可人工介入的工程化流程。

### 1. Temporal 工作流：把研发流程变成可靠流水线

控制平面的核心代码位于 `control-plane/devflow-engine/src/main/java/com/devflow/engine/workflow/DevFlowWorkflowImpl.java`。它定义了默认阶段顺序、Activity 调用方式、重试策略，以及人工审批的等待逻辑。

```java
private static final List<String> DEFAULT_STAGES = List.of(
    "REQUIREMENT_ANALYSIS",
    "SYSTEM_DESIGN",
    "CODE_GENERATION",
    "TEST_GENERATION",
    "APPLY_AND_RUN_TESTS"
);

public DevFlowWorkflowImpl() {
    this(Workflow.newActivityStub(
        DevFlowActivities.class,
        ActivityOptions.newBuilder()
            .setStartToCloseTimeout(Duration.ofMinutes(30))
            .setRetryOptions(RetryOptions.newBuilder()
                .setMaximumAttempts(5)
                .build())
            .build()
    ));
}
```

这段代码的意义不只是“按顺序调用几个函数”。Temporal 的价值在于，它把阶段执行变成可持久化的工作流：即使 Worker 重启，流水线状态仍然可以恢复；如果某个 Activity 因网络抖动或模型超时失败，Temporal 可以按配置重试；如果阶段需要人工审批，Workflow 可以暂停在检查点，等待用户发出 Approve 或 Reject 信号。

人工检查点也是项目里非常重要的一段代码：

```java
if (requiresHumanApproval(stageName)) {
    CheckpointSignal decision = waitForHumanDecision(stageName);
    while (decision.decision() == CheckpointDecision.REJECT) {
        stageResults.add(new StageExecutionResult(
            stageName,
            "REJECTED",
            Map.of("feedback", safeFeedback(decision.feedbackReason()))
        ));
        globalContext.put("human_feedback", safeFeedback(decision.feedbackReason()));
        globalContext.put("rejected_stage", stageName);

        result = executeStage(stageName, globalContext, previousOutput);
        previousOutput = result.outputPayload();
        decision = waitForHumanDecision(stageName);
    }
}
```

这段逻辑体现了 DevFlow-Engine 对“AI 生成结果不能直接放行”的设计态度。方案设计、代码生成、测试生成这些关键阶段默认需要人工审查。如果用户 Reject，反馈会被写回 `globalContext`，后续重新执行该阶段时 Agent 可以带着反馈修订结果。这让流水线不是一次性的线性生成，而是带有人类检查和回路修正的协作流程。

### 2. Temporal Activity 到 LangGraph 的桥接

控制平面负责调度，执行平面负责实际干活。Python 执行平面的入口位于 `execution-plane/src/workers/activities.py`。它把 Java 控制平面传来的 Activity 请求转换成 `DevFlowState`，再交给 LangGraph 阶段节点执行。

```python
@activity.defn(name="generateCode")
async def generate_code(request: StageExecutionRequest) -> StageExecutionResult:
    return _execute_stage(CODE_GENERATION, request)


def _execute_stage(stage_name: str, request: StageExecutionRequest) -> StageExecutionResult:
    try:
        state = _state_from_request(request)
        result_state = run_stage(stage_name, state)
        output_payload = _output_payload_for_stage(stage_name, result_state)
        return {
            "stageName": stage_name,
            "status": "COMPLETED",
            "outputPayload": output_payload,
        }
    except Exception as exc:
        raise concise_activity_error(stage_name, exc) from None
```

这里有两个设计点比较关键。

第一，Activity 的输出统一是 `StageExecutionResult`，里面带 `stageName`、`status` 和 `outputPayload`。这使 Java 控制平面不需要理解每个 Agent 的内部实现，只需要保存和转发阶段产物。

第二，异常会被包装成简短、可读的 `ApplicationError`。我们在真实 LLM 测试中遇到过 Temporal 报 `Failure exceeds size limit` 的问题，本质原因是异常链和模型响应太长，Temporal 失败 payload 超过限制。因此这里专门做了错误裁剪和提示：

```python
def concise_activity_error(stage_name: str, exc: Exception) -> ApplicationError:
    error_type = exc.__class__.__name__
    message = str(exc).strip() or error_type
    if len(message) > 1200:
        message = message[:1200] + "... [truncated]"
    hint = ""
    if error_type == "LlmTimeoutError" or "timed out" in message.casefold():
        hint = (
            "。这通常表示模型响应超过当前超时时间；可提高 DEVFLOW_LLM_TIMEOUT_SECONDS，"
            "或降低上下文/输出规模后重试。"
        )
    return ApplicationError(
        f"{stage_name} Activity failed: {error_type}: {message}{hint}",
        type=f"DevFlow{error_type}",
    )
```

这个改动很小，但对调试体验非常重要。它让用户看到的是“哪个阶段、什么异常、下一步可以怎么处理”，而不是一长串不可读的底层异常。

### 3. LangGraph 阶段图：让每个 Agent 成为独立节点

执行平面的阶段拓扑位于 `execution-plane/src/graph/flow.py`。LangGraph 在这里不是替代 Temporal，而是负责单次 Activity 内部的 Agent 节点执行。

```python
STAGE_ORDER = [
    REQUIREMENT_ANALYSIS,
    SYSTEM_DESIGN,
    CODE_GENERATION,
    TEST_GENERATION,
    APPLY_AND_RUN_TESTS,
    CODE_REVIEW,
    DELIVERY_INTEGRATION,
]


STAGE_NODES: dict[str, GraphNode] = {
    REQUIREMENT_ANALYSIS: analyze_requirement_node,
    SYSTEM_DESIGN: design_system_node,
    CODE_GENERATION: generate_code_node,
    TEST_GENERATION: generate_tests_node,
    APPLY_AND_RUN_TESTS: apply_and_run_tests_node,
    CODE_REVIEW: review_code_node,
    DELIVERY_INTEGRATION: integrate_delivery_node,
}
```

阶段执行时，`run_stage` 会找到对应节点，把原始状态和节点输出合并：

```python
def run_stage(stage_name: str, state: DevFlowState) -> DevFlowState:
    try:
        node = STAGE_NODES[stage_name]
    except KeyError as exc:
        raise ValueError(f"Unsupported DevFlow stage: {stage_name}") from exc

    updates = node(state)
    merged: dict[str, Any] = dict(state)
    merged.update(updates)
    return merged
```

这个结构让每个 Agent 的职责比较清楚：Requirement Agent 只产出 `structured_prd`，Design Agent 只产出 `design_doc`，Coder Agent 只产出 `diff_patch` 和 `code_generation_report`。阶段之间通过状态字段连接，而不是互相直接调用内部方法。

### 4. 可配置 LLM Client：把模型调用从 Agent 中抽出来

项目没有让每个 Agent 直接调用某个厂商 SDK，而是在 `execution-plane/src/llm/client.py` 中实现统一的 `LlmClient`。这样做的好处是模型提供商、模型名称、超时、重试、JSON 解析、日志追踪都可以统一管理。

```python
class LlmClient:
    """Agent 调用 LLM 的唯一门面。"""

    @staticmethod
    def default_providers() -> dict[str, LlmProvider]:
        return {
            "openai_compatible": OpenAICompatibleProvider(),
            "anthropic_compatible": AnthropicCompatibleProvider(),
            "fake": FakeProvider(),
        }

    def complete_json(self, request: LlmRequest) -> dict[str, Any]:
        json_request = replace(request, response_format="json")
        response = self.complete(json_request)
        if response.parsed_json is None:
            raise LlmJsonParseError("LLM response did not contain parsed JSON")
        return response.parsed_json
```

这段代码背后有一个重要思想：Agent 不应该关心 API Key 放在哪里，也不应该关心调用的是 OpenAI-compatible 还是 Anthropic-compatible。Agent 只需要描述任务、输入消息、输出 schema。这样未来替换模型、做 A/B 测试、针对代码生成阶段使用更强模型，都会更容易。

LLM Client 还负责记录请求和响应：

```python
self.trace_recorder.record(
    "llm.request",
    {
        "task": request.task,
        "provider": provider_name,
        "model": request.model
        or request_config.default_model
        or provider_settings.get("default_model"),
        "timeout_seconds": effective_timeout_seconds,
        "messages": [
            {"role": message.role, "content": message.content}
            for message in request.messages
        ],
        "json_schema": request.json_schema,
    },
)
```

这个功能是在真实调试中逐步补出来的。因为只知道“模型失败”是不够的，用户还需要知道模型到底收到了什么 prompt、返回了什么内容、JSON 为什么解析失败、diff 为什么不能应用。

### 5. 渐进式代码探索：不是一次性塞完整仓库

项目最重要的 AI 基础能力之一，是 `execution-plane/src/context/repository_context.py` 中的代码库上下文探索。它的目标不是把整个仓库全部读给模型，而是像 Codex / Claude Code 一样，逐步查看项目结构、搜索关键词、读取少量高相关文件，并在预算内形成证据。

```python
@dataclass(frozen=True)
class ExplorationBudget:
    """一次渐进探索的硬预算。

    这些字段不是给 LLM 的建议，而是工具层必须执行的上限。
    """

    max_rounds: int = DEFAULT_MAX_ROUNDS
    max_files: int = DEFAULT_MAX_FILES
    max_bytes: int = DEFAULT_MAX_BYTES
    max_searches: int = DEFAULT_MAX_SEARCHES
    max_search_results: int = DEFAULT_MAX_SEARCH_RESULTS
```

这段预算控制非常关键。真实项目可能有几千个文件，如果一开始就把所有候选文件 excerpt 给模型，会导致 token 成本高、响应慢、甚至超时。我们后来将路径驱动工具升级为“工具调用式渐进探索 Agent”，默认只要求用户提供仓库根目录，`include/exclude/targetFiles` 变成高级选项。

探索请求也体现了安全边界：

```python
@dataclass(frozen=True)
class RepositoryExplorationRequest:
    """渐进式代码感知入口请求。

    常规用户只需要 root_path。include/exclude/target/budget 都是高级约束，
    它们只能收窄或优先排序探索范围，不能扩大 root_path 之外的访问权限。
    """

    root_path: Path
    include_paths: tuple[str, ...] = ()
    exclude_paths: tuple[str, ...] = ()
    target_files: tuple[str, ...] = ()
    budget: ExplorationBudget = field(default_factory=ExplorationBudget)
    privacy_mode: str = "standard"
```

这个设计让普通用户不需要手写大量路径规则，同时又保留了高级控制能力：用户如果想保护隐私、提高速度、限制范围，可以主动填写 include/exclude。

### 6. Requirement Agent：需求分析和代码证据结合

Requirement Agent 位于 `execution-plane/src/agents/requirement_agent.py`。它不是规则模板分析器，而是使用 LLM 生成结构化 PRD，同时结合代码探索结果。

```python
class RequirementAgent:
    """把自然语言需求转换为结构化 PRD 的执行平面 Agent。

    这里没有实现任何规则模板型 Requirement Analyzer。固定逻辑只负责准备
    输入、收集有限代码上下文、调用 T021 LLM Client、校验输出结构；真正的
    PRD 内容来自 LLM 或测试注入的 Fake Provider。
    """
```

在上下文收集时，Agent 会优先复用前序阶段已经探索过的代码上下文：

```python
reusable_code_context = select_reusable_code_context(pipeline_context, repository_payload)
if reusable_code_context:
    context_pack = context_pack_from_reused_code_context(reusable_code_context)
    self.llm_client.trace_recorder.record(
        "requirement_agent.context_reuse",
        {
            "source_stage": reusable_code_context.get("stage"),
            "inspected_files": reusable_code_context.get("inspected_files", []),
            "confidence": reusable_code_context.get("confidence", 0.0),
        },
    )
    return {"context_pack": context_pack}
```

这是我们在开发过程中专门补上的设计。因为如果每个阶段都重新探索一遍仓库，不仅浪费 token 和时间，还可能让不同阶段看到的上下文不一致。通过 `pipeline_context` 共享代码证据，后续 Agent 可以在已有上下文上继续工作。

### 7. Coder Agent：生成 diff，但不直接修改文件

代码生成阶段位于 `execution-plane/src/agents/coder_agent.py`。这里的边界非常明确：Coder Agent 只生成可审查的 unified diff，不直接写入文件，不运行命令，不提交 Git。

```python
class CoderAgent:
    """负责 CODE_GENERATION 阶段的 LangGraph 子图。

    CoderAgent 的输出边界非常明确：它只生成可审查的 unified diff 文本和结构化
    code_generation_report，不直接写文件、不运行命令、不提交 Git。
    """
```

Coder Agent 的子图结构如下：

```python
builder.add_node("prepare_input", agent.prepare_input)
builder.add_node("plan_code", agent.plan_code)
builder.add_node("draft_code_patch", agent.draft_code_patch)
builder.add_node("validate_patch", agent.validate_patch)
builder.add_node("repair_patch", agent.repair_patch)
builder.add_node("finalize", agent.finalize)
builder.add_node("fail_soft", agent.fail_soft)
```

这段设计把“生成代码”拆成了几个可诊断步骤：先准备输入，再规划目标文件，再调用 LLM 生成 diff，再校验补丁结构。如果补丁只是外壳格式有问题，例如被包在 Markdown code block 里，可以做一次轻量修复；如果 hunk 行数不一致，就进入 `fail_soft`，输出可读诊断。

我们在真实测试中遇到过 `git apply` 报 `corrupt patch at line 25`。因此后来专门加入了 unified diff 结构校验：

```python
def validate_patch_payload(value: dict[str, Any]) -> list[str]:
    issues: list[str] = []
    diff_patch = str(value.get("diff_patch") or "")
    if not diff_patch.strip():
        issues.append("diff_patch is required")
    if diff_patch.strip() and not looks_like_unified_diff(diff_patch):
        issues.append("diff_patch must be a unified diff")
    if diff_patch.strip():
        issues.extend(validate_unified_diff_structure(diff_patch))
    if contains_forbidden_execution_text(diff_patch):
        issues.append("diff_patch must not contain command execution instructions")
    return issues
```

更进一步，失败报告会带上 LLM 原始输出和校验结果：

```python
def build_llm_diagnostics(state: CoderAgentState) -> dict[str, Any]:
    return {
        "attempts": int(state.get("attempts", 0)),
        "raw_model_output": redact_large_strings(state.get("draft_patch") or {}),
        "normalized_patch": redact_large_strings(state.get("normalized_patch") or {}),
        "validation_report": redact_large_strings(state.get("validation_report") or {}),
        "code_plan": redact_large_strings(state.get("code_plan") or {}),
    }
```

这段代码是从真实问题中演进出来的。最开始只显示“无法生成代码 diff”，用户无法判断模型到底返回了什么。现在报告中会包含 `raw_model_output`、`normalized_patch` 和 `validation_report`，前端也会单独展示“LLM 诊断”区块。

### 8. 流水线控制台：把阶段产物给人看

前端控制台位于 `sandbox/frontend/src/main.ts` 和 `sandbox/frontend/src/viewModel.ts`。它不是简单的表单页面，而是围绕流水线测试设计的工作台：左侧创建流水线，中间查看状态和阶段，右侧查看结构化产物，下方进行人工审批。

```typescript
const AUTO_REFRESH_INTERVAL_MS = 3000;

const state: AppState = {
  pipeline: null,
  selectedStageName: null,
  lastCheckpointOutput: null,
  checkpointOutputExpanded: false,
  loading: false,
  message: null,
  error: null,
};
```

页面默认填写了本地项目路径和测试需求，便于用户直接在 5173 端口发起一次真实流水线：

```typescript
<textarea name="requirement" required>
完成 DevFlow-Engine 阶段 4 的 T030 任务。请在根目录的test文件夹下生成一个简单的测试网页和插件代码。
</textarea>
```

阶段产物展示由 `viewModel.ts` 负责，不同阶段展示不同核心字段。例如需求分析只展示 `structured_prd`，方案设计只展示 `design_doc`，代码生成展示 diff 和代码生成报告，避免把所有 JSON 混在一起让用户自己找重点。

代码 diff 也做了更直观的展示：每个文件开头显示类似 `Edited specs\001-devflow-engine\execution-plane-architecture.md (+36 -3)` 的摘要，新增行使用绿色背景，删除行使用红色背景。这一点参考了 Codex 类工具的体验，让用户能快速判断 AI 到底改了什么。

### 小结

这些核心代码共同体现了 DevFlow-Engine 的基本思路：Temporal 管长周期可靠流程，LangGraph 管单次阶段内的 Agent 状态流转，LLM Client 管模型调用，Repository Context 管代码感知，前端控制台管人机协作和产物展示。项目最核心的价值不是“调用一次大模型”，而是把 AI 研发过程拆成可观察、可审查、可重试、可继续演进的工程系统。
