# 3）项目亮点介绍

DevFlow-Engine 的目标不是做一个简单的聊天机器人，也不是把需求直接丢给大模型生成一段代码。它更像是一套“AI 研发流水线引擎”：用户提出需求后，系统会依次完成需求分析、方案设计、代码生成、测试生成、应用代码并运行测试、代码评审和交付集成。每个阶段都有独立产物，也都能被前端控制台查看和人工检查。

从实现过程看，项目的亮点主要集中在可靠流程编排、代码上下文感知、阶段产物可视化、真实环境测试和文档化工程实践几个方面。

## 1. 用 Temporal 承担可靠的流程编排

项目最基础的亮点是引入 Temporal 作为控制平面的工作流引擎。AI 研发流程并不是一个很短的请求，它可能经历多次模型调用、人工审批、失败重试、测试执行和状态查询。如果只用普通 HTTP 请求串起来，一旦服务重启、网络超时或者某个阶段卡住，恢复成本会非常高。

Temporal 解决的是这个长周期流程的可靠性问题。DevFlowWorkflowImpl 会把流水线拆成多个 Activity，每个 Activity 对应一个研发阶段。它提供了几个直接价值：

- **持久化**：Workflow 状态由 Temporal Server 管理，Worker 重启后仍然可以继续。
- **重试配置**：每个 Activity 配置最大重试次数，避免一次 LLM 超时就导致整条流水线彻底失败。
- **人工检查点**：方案设计、代码生成、测试生成等阶段可以暂停，等待用户 Approve 或 Reject。
- **状态查询**：前端可以随时查询 Workflow 当前阶段和历史阶段产物。

在真实测试过程中，Temporal 的价值非常明显。我们遇到过 Worker 没启动、多个 Worker 同时运行、Activity 超时、失败 payload 过大等问题。正因为引入了 Temporal，这些问题都可以被定位到具体 Task Queue、具体 Activity 和具体 Workflow 状态，而不是变成一团难以追踪的后台任务。

## 2. Temporal 可视化让流水线运行过程更透明

Temporal Web UI 也是项目的一个亮点。用户可以看到 Workflow 是否 Running、Suspended、Completed 或 Failed，也可以看到某个 Activity 是否在重试。

例如当 8234 端口显示 `No Workers Running` 时，我们就能直接判断不是前端请求错了，也不是数据库坏了，而是没有 Worker 正在轮询 `DEVFLOW_TASK_QUEUE`。这种可视化能力让部署和调试的成本明显降低。

同时，控制平面还把前端填写的流水线名称作为 Workflow ID 的前缀。这样在 Temporal UI 中，不再只有一串 UUID，而是能看到和业务需求相关的名字。对于持续测试很多条流水线的场景，这个体验很实用。

## 3. 渐进式探索提升代码检索能力

项目一开始的代码感知能力是路径驱动的：用户提供根目录、包含路径、排除路径和重点文件，系统根据这些路径读取上下文。这个方式可控，但对普通用户不够友好。后来我们把它升级为“路径驱动 + 工具调用式渐进探索”的方案。

新的设计默认只要求用户提供仓库根目录，include/exclude 不再是必填项，而是高级选项。系统会先生成 compact repository map，了解项目有哪些目录、语言、入口文件和高信号文件；再根据需求生成搜索词；然后通过 list、search、read 等工具逐步查看相关代码。

这个设计有几个好处：

- 不需要用户提前知道哪些文件最相关。
- 不会一次性把整个仓库塞给模型，避免 token 成本过高。
- 每次探索都有预算限制，例如最大轮次、最大文件数、最大字节数。
- 读取过的文件、搜索词、证据和跳过路径都会被记录，方便用户回看。
- 后续阶段可以复用前面阶段的代码上下文，减少重复探索。

在真实项目里，这一点非常重要。DevFlow-Engine 自身就包含 Java 控制平面、Python 执行平面、TypeScript 前端和大量 Markdown 设计文档。如果没有渐进式探索，模型很容易读到大量不相关文件，或者因为上下文太大而超时。

## 4. 流水线控制台前端页面

前端控制台是项目从“后端能力”走向“用户可测试产品”的关键部分。它运行在 5173 端口，用户可以直接在页面上创建流水线、填写需求、配置代码库路径、选择阶段、刷新状态、查看产物并提交人工审批。

控制台的设计重点不是做一个宣传页，而是做一个真正能反复使用的测试工作台。页面里有几类核心区域：

- **创建流水线**：填写流水线名称、需求描述、阶段选择和代码库上下文。
- **状态与产物**：展示 Pipeline ID、Workflow ID、当前阶段和每个阶段状态。
- **阶段产物**：根据阶段类型展示结构化 PRD、设计文档、代码 diff、测试结果、评审报告等。
- **人工检查点**：用户可以 Approve 或 Reject，并填写反馈。

我们在迭代中专门优化了产物展示方式。需求分析阶段只展示 `structured_prd`，方案设计阶段只展示 `design_doc`，代码生成阶段展示完整 diff 和 `code_generation_report`，而不是把所有 JSON 原样堆在页面上。这样用户能真正看懂每个阶段发生了什么。

代码 diff 的展示也做了视觉优化：每个文件有类似 Codex 的文件摘要，新增行绿色、删除行红色，并且展示完整 diff，而不是只展示片段。这对人工检查很关键，因为代码生成阶段默认需要用户审批。

## 5. 中间产物落库和可追踪

项目的另一个亮点是重视中间产物。每个阶段不只是返回一个“成功/失败”，还会返回面向用户和后续 Agent 的结构化产物：

- `structured_prd`：需求分析结果。
- `code_context`：代码探索证据。
- `design_doc`：方案设计文档。
- `diff_patch` 和 `code_generation_report`：代码生成结果。
- `test_results`：测试生成结果。
- `test_run_results`：应用代码和运行测试结果。
- `review_report`：代码评审报告。
- `delivery_status`：交付集成状态。

这种设计的好处是，每个阶段的输出都可以被人检查，也可以被后续阶段消费。比如 Design Agent 的 `file_plan` 会成为 Coder Agent 生成 diff 的依据；Requirement Agent 的 `code_context` 会成为后续阶段判断项目结构的依据；测试执行结果会影响代码评审和交付集成。

我们在实际调试中也发现，中间产物越清楚，定位问题越快。例如代码生成阶段失败时，原来只显示 `BLOCKED`，无法判断模型到底返回了什么。后来我们补充了 `llm_diagnostics.raw_model_output`、`normalized_patch` 和 `validation_report`，用户就能直接看到是模型没有返回 JSON、返回了空 diff，还是 hunk 行数不一致。

## 6. TDD 和真实测试并重

项目开发过程中一直强调 TDD。每个 Agent 在实现时都尽量先写测试，再补实现。测试里使用 Fake Provider 模拟 LLM 输出，保证离线测试稳定，不依赖真实 API Key 和网络。

但项目也没有只停留在 Fake 测试。我们后续加入了真实 LLM 冒烟测试、`.env.local` 本地密钥配置、LLM trace 日志、前后端真实联调、WSL Docker 容器里的 Temporal/Postgres 部署，以及本机 Maven 的控制平面测试。

这种组合比较务实：

- 单元测试保证核心逻辑稳定。
- Fake Provider 保证 Agent 行为可复现。
- 真实 LLM 测试暴露模型输出不稳定、超时、JSON 解析失败等真实问题。
- 前端控制台测试验证用户是否真的能跑通流程。

例如 `LlmJsonParseError`、`LlmTimeoutError`、`corrupt patch at line 25` 这些问题，都是在真实测试中暴露出来的。如果只写离线单元测试，很难提前发现这些工程细节。

## 7. 控制平面和执行平面解耦

DevFlow-Engine 采用 Java 控制平面 + Python 执行平面的结构。Java Spring Boot 更适合写 REST API、数据库模型、Temporal Workflow 和控制台对接；Python 更适合写 LangGraph Agent、LLM Client、代码上下文工具和测试脚本。

两个平面之间通过 Temporal Activity 契约和 JSON 结构通信，而不是互相直接调用内部代码。这种方式虽然前期需要更多契约设计，但长期更清晰：

- 控制平面关心流水线状态、数据库和 Workflow。
- 执行平面关心 Agent、模型调用和代码工具。
- 前端只关心 REST API 和阶段产物展示。

这也让项目更容易扩展。后续如果要换模型、增加 Agent、调整前端展示，通常不需要同时大改所有层。

## 8. 文档化程度高，适合持续演进

项目开发过程中生成和维护了大量文档，包括 `spec.md`、`plan.md`、`tasks.md`、`agent-design.md`、`control-plane-architecture.md`、`execution-plane-architecture.md`、`t021-llm-client-design.md`、`t022-requirement-agent-design.md` 等。

这些文档不是事后补的说明，而是参与了实际开发流程。很多任务都是先讨论设计文档，再实现代码，再写测试。比如渐进式探索循环、LLM Client、Requirement Agent、Design Agent、Coder Agent 都经历了“先设计、再实现、再真实测试、再修正”的过程。

这也是 AI 项目很重要的工程习惯。因为 AI 系统的不确定性比普通业务系统更高，如果没有文档记录设计边界、状态字段、失败策略和人工检查点，项目很快会变得难以维护。

## 小结

DevFlow-Engine 的项目亮点不在于某一个单独模块，而在于把多个工程能力组合成一套完整流水线：Temporal 保证流程可靠，LangGraph 组织 Agent，渐进式探索提供代码感知，LLM Client 屏蔽模型差异，前端控制台把中间产物展示给用户，TDD 和真实测试保证系统能落地运行。它更像是一个面向 AI 软件工程流程的基础平台，而不是一次性的代码生成脚本。
