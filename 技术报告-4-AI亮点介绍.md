# 4）AI 亮点介绍

DevFlow-Engine 的 AI 亮点主要体现在“怎么把大模型真正放进软件工程流程里”。项目没有把 AI 当成一个黑盒接口，而是围绕需求、设计、代码、测试、评审和交付建立了一套可控的工作流。简单说，它不是让 AI 一次性回答“帮我写代码”，而是让 AI 在一个有状态、有边界、有人工审查、有测试反馈的系统里工作。

## 1. Spec-Kit 带来的文档驱动开发方式

项目开发过程中大量使用了 Spec-Kit 思路。它把自然语言需求拆成几个连续步骤：constitution、spec、plan、tasks、implement。这个流程改变了传统“想到哪里写到哪里”的开发方式。

在 DevFlow-Engine 中，很多功能不是直接开始写代码，而是先形成文档：

- `spec.md` 描述需求和验收标准。
- `plan.md` 描述技术方案。
- `tasks.md` 拆成可执行任务。
- `agent-design.md` 描述各个 Agent 的职责、输入输出和状态字段。
- 架构文档记录控制平面、执行平面、前端控制台和数据模型。

这种方式对 AI 协作特别有价值。因为 AI 需要明确上下文和目标，如果只有一句“完成下一个任务”，模型容易偏离方向；但如果任务已经被写入 `tasks.md`，设计边界也写在文档里，AI 就更容易生成符合项目风格的代码。

从实际体验看，Spec-Kit 的价值不只是生成文档，而是让“需求、设计、任务、代码、测试”之间建立了可追踪关系。后续出现问题时，也可以回到文档中判断是需求不清、设计不合理，还是实现偏离了计划。

## 2. 人和 AI 的分工：人做 Reviewer / Tester，AI 做 Coder

这个项目中，人和 AI 的分工比较明确。人的角色更像 Reviewer、Tester 和产品判断者；AI 的角色更像 Coder、文档助手和流水线执行者。

人的主要工作包括：

- 提出需求和约束，比如“不直接提交代码”“需要 TDD”“需要中文注释”“需要真实测试”。
- 检查 AI 生成的设计是否合理。
- 在前端控制台审批方案、代码 diff 和测试结果。
- 发现真实测试中的问题，比如 timeout、JSON 解析失败、diff 损坏、页面卡顿。
- 判断模型输出是否符合实际开发体验。

AI 的主要工作包括：

- 阅读代码库和文档，理解已有架构。
- 根据任务实现代码。
- 编写单元测试和部分真实测试脚本。
- 补充架构文档、Agent 设计文档和测试说明。
- 根据真实错误日志定位问题并修复。

这种分工比较符合当前 AI 编程工具的实际能力。AI 很适合处理重复性实现、根据已有模式补代码、生成测试和文档；但对需求优先级、产品体验、模型输出质量、人机交互是否合理这些问题，仍然需要人来判断。

项目里人工检查点的设计也体现了这一点。方案设计、代码生成、测试生成不是自动一路放行，而是默认需要人工审批。这样既能利用 AI 的生成能力，也能避免 AI 错误直接进入后续阶段。

## 3. Temporal + LangGraph 的选型思路

项目中一个核心选型是 Temporal + LangGraph。两者职责不同，不能简单互相替代。

Temporal 负责长周期流程：

- 哪个阶段先执行，哪个阶段后执行。
- Activity 是否需要重试。
- Workflow 状态如何持久化。
- 人工检查点如何暂停和恢复。
- 前端如何查询当前流水线状态。

LangGraph 负责单个阶段内部的 Agent 状态流转：

- Agent 如何准备输入。
- 如何规划任务。
- 如何调用 LLM。
- 如何校验模型输出。
- 如果输出有小问题，是否进入修复节点。
- 失败时如何生成可诊断报告。

以 Coder Agent 为例，它内部有 `prepare_input -> plan_code -> draft_code_patch -> validate_patch -> repair_patch/finalize/fail_soft` 这样的子图。这种细粒度状态转移更适合 LangGraph；而“代码生成阶段执行完成后是否进入人工审批、是否继续测试生成”则更适合 Temporal。

因此项目并不是重复使用两个状态机，而是把状态管理拆成两层：Temporal 管流水线级状态，LangGraph 管 Agent 内部状态。

## 4. 可配置 LLM Client：避免绑定单一模型

项目实现了统一的 LLM Client，支持至少两个 Provider：OpenAI-compatible 和 Anthropic-compatible，并且保留 Fake Provider 做测试。这个设计是 AI 工程化的重要部分。

它解决了几个实际问题：

- API Key 不写死在代码里，而是通过 `.env.local` 或配置文件注入。
- 不同 Provider 的请求和响应格式不同，但 Agent 看到的是统一接口。
- 可以为不同任务配置不同 timeout。
- 可以记录 LLM 请求和响应，方便调试。
- 单元测试可以使用 Fake Provider，不依赖真实网络。
- JSON 输出失败时可以记录原始响应预览，帮助判断模型到底返回了什么。

在真实开发中，我们确实遇到了模型超时、JSON 解析失败、返回非 JSON、diff 格式损坏等问题。如果每个 Agent 自己写模型调用逻辑，这些错误处理会分散在各处，很难统一修复。统一 LLM Client 让这些能力集中起来，也让后续替换更强模型变得更容易。

## 5. 结构化输出和 JSON Schema 思路

项目中的 Agent 不只是让模型返回一段自然语言，而是尽量要求结构化 JSON 输出。例如 Requirement Agent 输出 `structured_prd`，Design Agent 输出 `design_doc`，Coder Agent 输出 `diff_patch` 和 `code_generation_report`，Review Agent 输出 `review_report`。

结构化输出的价值很直接：

- 前端可以按字段展示，而不是展示一整段不可解析文本。
- 后续 Agent 可以直接消费前一阶段的产物。
- 测试可以断言字段是否存在、数量是否足够、语言是否一致。
- 失败时可以明确知道是缺字段、类型错误，还是模型没有按格式输出。

当然，结构化输出也带来了新的问题：模型有时仍然会返回 Markdown、说明文字或不合法 JSON。因此项目在 LLM Client 里做了有限 JSON 修复，在 Agent 里做结构校验。这里的原则是：可以修复格式外壳，但不应该用规则自动编造业务内容。比如 Requirement Agent 不再支持 `RuleBasedRequirementAnalyzer`，因为那会让系统退回模板生成，而不是由 LLM 完成真实分析。

## 6. 渐进式代码探索：接近 Codex / Claude Code 的工作方式

项目中非常重要的 AI 技巧是“渐进式披露”的代码感知方式。最开始如果要求用户手动填写 include/exclude 路径，体验会比较像传统配置工具；后来升级后，普通用户只需要提供仓库根目录，Agent 会自己逐步探索。

这个探索方式大致包括：

1. 读取 compact repository map，先知道仓库有哪些目录、语言和入口文件。
2. 根据需求让 LLM 生成搜索词，而不是用简单规则拆词。
3. 通过搜索和路径评分找到候选文件。
4. 只读取少量高相关文件片段，形成 evidence。
5. 根据 evidence、open questions 和 confidence 判断是否继续探索。
6. 在预算耗尽或置信度足够时退出。

这比“一次性读取所有文件”更符合真实 AI 编程工具的做法。它既节省 token，也能减少模型被无关内容干扰。更重要的是，探索过程会留下 `exploration_trace`、`search_queries`、`inspected_files` 和 `evidence`，用户可以看到模型为什么认为某些文件相关。

我们在开发中也讨论过是否把 compact repo map 做三层缓存，最终暂时没有做复杂缓存，而是先维持原设计。这体现了项目的一个务实取舍：先把正确的循环和可观测性做好，再进一步优化性能。

## 7. 中间产物和可观察性

AI 系统最怕的问题之一是“失败了，但不知道为什么”。DevFlow-Engine 在开发过程中逐步加强了可观察性。

例如 LLM 调用现在可以输出 trace 日志，记录消息、schema、模型、timeout 和响应。前端控制台可以展示阶段产物，人工检查点可以查看提交响应。代码生成失败时，`code_generation_report` 会包含 `llm_diagnostics`，里面有原始模型输出、规范化补丁、校验报告和代码计划。

这个能力不是一开始就设计完美的，而是在真实测试中一点点补出来的。我们遇到过这些问题：

- 模型请求超时，但 Temporal 返回的信息太长。
- JSON 解析失败，只看到 `line 1 column 1`，不知道模型原文是什么。
- 代码生成阶段失败，只显示 `BLOCKED`，不知道 diff 是空的还是损坏的。
- `git apply` 报 `corrupt patch at line 25`，需要提前在 Coder Agent 阶段校验。

这些问题推动项目加入更清晰的错误提示、更短的 Activity 异常、更完整的 LLM 诊断和前端展示。对 AI 工程来说，可观察性不是锦上添花，而是能不能持续迭代的基础。

## 8. 模型选型：不同阶段需要不同能力

项目目前支持可配置模型，但从实际经验看，不同阶段对模型能力的要求并不一样。

需求分析和方案设计更需要长上下文理解、结构化表达和中文输出稳定性。代码生成阶段则对模型的代码能力和格式遵循要求更高，尤其是 unified diff 这种格式，对 hunk 行数和上下文非常敏感。测试生成阶段需要理解测试框架和项目运行方式。评审阶段则更像代码审查，需要发现风险、缺失测试和边界场景。

因此后续更合理的方向是支持按 task 配置模型。例如：

- `requirement_analysis` 使用理解能力强、成本适中的模型。
- `system_design` 使用长上下文和结构化输出较强的模型。
- `code_generation` 使用更强的代码模型。
- `code_review` 使用擅长审查和推理的模型。

不过，我们也在真实问题中看到，仅仅换更强模型并不能解决所有问题。比如让模型直接手写 unified diff，即使用强模型，也仍然可能出现 hunk 行数不一致。更稳的长期方案是让模型输出结构化文件编辑计划或完整文件内容，再由本地程序生成 diff。也就是说，模型能力和工程约束要一起设计，而不是只依赖模型本身变强。

## 9. Hook 和端到端测试思路

项目中还引入了 Hook / e2eTest 的思路。它的核心价值是把 AI 生成结果放到真实流程中验证，而不是只看文本是否“看起来正确”。

例如前端控制台不是只写了组件代码，而是实际运行前后端，创建流水线，查看状态变化和阶段产物。LLM 测试也不是只用 Fake Provider，而是提供 `.env.local` 和真实网络冒烟脚本，让用户可以带着自己的 API Key 做一次真实调用。

这种端到端测试对 AI 项目尤其重要，因为很多问题只有在真实链路里才会出现：Worker 没启动、端口冲突、Temporal Task Queue 没有 poller、LLM timeout、Windows 路径中文处理、前端 JSON 展示卡顿、diff 应用失败等。项目开发过程中多次通过真实测试发现问题，再回到代码和文档中修复。

## 10. AI 对原有工作流带来的改变

引入 AI 后，项目开发方式发生了几个明显变化。

第一，工作流变得更文档化。以前可能是直接写代码，现在会先写需求、方案、任务和设计文档。Markdown 不再只是说明书，而是 AI 和人共同协作的上下文。

第二，TDD 更重要。因为 AI 生成代码速度快，但也更容易产生看似合理、实际错误的实现。先写测试可以给 AI 明确目标，也能防止后续修改破坏已有行为。

第三，注释和可读性更重要。项目里要求 Agent、工具调用、状态流转和复杂模块保留中文注释。这不是为了凑字数，而是因为 AI 参与后，代码会被更多“后续 Agent”和人类读到，清楚的注释能减少误解。

第四，人类审批被前置到流程中。AI 不是直接把代码写进主分支，而是先输出 PRD、设计文档、diff 和测试结果，用户可以在控制台检查后再决定是否继续。

第五，调试方式从“看最终结果”变成“看每一步产物”。当结果不符合预期时，我们会查看 LLM 消息、上下文证据、阶段产物、validation_report、Temporal Activity 状态和前端展示，而不是只重新问一遍模型。

## 小结

DevFlow-Engine 的 AI 亮点不只是“用了大模型”，而是围绕大模型建立了一套工程化约束：文档驱动、结构化输出、渐进式上下文、可配置模型、可诊断日志、人工检查点、TDD 和真实端到端测试。它把 AI 从一个一次性生成工具，推进到可以参与软件研发流程的流水线成员。人仍然负责判断和审查，AI 负责生成、整理和执行，两者通过文档、测试和控制台协作。
