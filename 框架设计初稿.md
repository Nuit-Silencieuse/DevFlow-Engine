这份设计框架文档融合了开源流水线的最优实践与此前推演的极致容错架构。它抛弃了传统脆弱的同步调用或简单的状态机，全面拥抱「持久化执行（Durable Execution）」与「状态化图计算（Stateful Graph）」范式。

------

# DevFlow Engine：AI Native 研发全流程引擎设计蓝图

## 一、 架构愿景与核心选型

本系统旨在构建一个高度容错、可追溯且具备极致交互体验的 AI 驱动软件研发流水线。核心基建分为三大平面：负责生命周期与状态管理的**控制平面**，负责大语言模型（LLM）复杂推理与工具调用的**执行平面**，以及负责实现“所见即所得”编辑的**本地沙箱平面**。

### 核心技术栈组合

- **控制平面 (Orchestration Plane)：** Java (Spring Boot) + Temporal
- **执行平面 (Execution Plane)：** Python (FastAPI) + LangGraph
- **数据检索与存储：** Milvus (向量引擎) + PostgreSQL (持久化状态) + Elasticsearch (代码检索引擎)
- **交互沙箱 (Sandbox Plane)：** React/Vite + Node.js/Go Local Daemon

------

## 二、 核心模块详细设计

### 1. 控制平面：基于 Temporal 的全局引擎 (Java + Spring Boot)

抛弃传统基于 Redis + 线程池的手写状态机，直接将 Temporal 作为 Pipeline 引擎的底座。控制平面的核心职责是“调度、挂起、恢复与兜底”，不涉及具体的 AI 提示词工程。

- **工作流定义 (Workflow)：** 使用 Java SDK 定义 `DevFlowWorkflow`。将“需求 → 方案 → 编码 → 测试 → 评审 → 交付”这六大阶段定义为线性的代码逻辑。由于 Temporal 的 Durable Execution 特性，代码中可以直接使用阻塞调用（如等待人工审批）而不用担心服务重启导致状态丢失。
- **活动节点 (Activity)：** 每一个阶段的具体执行被定义为一个 Activity。Java 端只定义 Activity 的接口契约（输入：上下文；输出：生成产物），具体的执行交由 Python 侧的 Worker 认领。
- **人工干预机制 (Human-in-the-Loop)：**
  - **挂起：** 工作流执行完某个 Activity 后，进入 `Workflow.await()` 状态。
  - **唤醒：** 前端 UI 通过 Spring Boot 暴露的 RESTful API 发起审批，Spring Boot 转化为 Temporal 的 `Signal` 发送给运行中的工作流。
  - **处理：** 接收到 Approve 信号，继续下一阶段；接收到 Reject 信号（携带拒绝理由），触发补偿事务（Compensation Transaction）回滚到上一 Activity。
- **高并发与隔离：** 利用 Temporal 底层机制处理多任务排队与并发，并对 LLM 接口调用设置指数退避重试（Exponential Backoff）和死信流转。

### 2. 执行平面：基于 LangGraph 的智能体工作站 (Python)

接收来自 Temporal 的任务，利用其强大的 Python AI 生态进行任务拆解与生成。

- **统一共享状态 (State TypedDict)：** 在 LangGraph 中定义一个包含完整研发周期的全局状态对象（如包含 `prd`, `design_doc`, `diff_patch`, `test_results`）。
- **多智能体拓扑图：** 将六个阶段映射为图的节点（Nodes）。在特定节点（如代码生成节点）内部，可以进一步拆解为一个子图（Sub-Graph），例如“架构师 Agent 检索向量库 -> 程序员 Agent 写代码 -> 审查员 Agent 检查语法”。
- **时间旅行与精确回溯 (Time Travel)：** 配合 Temporal 的 Reject 信号，LangGraph 利用其自带的 Checkpointer。当人类打回代码时，Python 侧不仅仅是简单重试，而是**将状态图回拨到代码生成前的时间节点**，将人类的修改意见注入当前上下文中，进行有针对性的修复生成。
- **RAG 与代码库索引：** 结合 Milvus 和 Elasticsearch。系统启动或代码库变更时，触发索引任务，将代码 AST 结构和文档块向量化。方案设计 Agent 通过 LangChain 的 Retriever 组件召回相关代码上下文。

### 3. 沙箱平面：AST 外科手术与本地交互 (前端拓展)

负责解决“网页圈选 -> 源码映射 -> 热更新 -> 自动 MR”的核心闭环。

- **源码空间映射 (Babel 注入)：** 利用 Vite 构建体系，在开发环境下注入 `@babel/plugin-transform-react-jsx-source` 插件。为页面所有 DOM 节点自动挂载 `__source={{fileName, lineNumber}}` 属性。
- **悬浮中枢 (Content Script)：** 注入到目标页面的 React 组件。实现鼠标 Hover 高亮、框选，并提取选中 DOM 的 `__source` 信息。结合用户输入的自然语言修改指令，打包发送给本地守护进程。
- **本地守护进程 (Local Daemon)：**
  - 接收到 `fileName` 和修改指令，读取本地磁盘对应的源码文件。
  - 通过独立的 LLM 调用（或转交 Python 端处理），精准定位 AST 节点并进行代码替换。
  - 覆写本地文件。依赖底层 Vite 的模块热替换 (HMR)，实现页面不刷新即看效果。
- **GitOps 交付：** 用户确认无误后，Daemon 调用 `Hub4j` 或本地 Git CLI，执行 `checkout -b`, `commit`, 并通过 API 发起 Merge Request。

------

## 三、 全链路系统交互时序

1. **启动：** 用户在前端提交一句话需求。Spring Boot 接收请求，启动 `DevFlowWorkflow` 实例。
2. **流转：** Temporal 调度第一阶段 `RequirementAnalysisActivity`。部署在另一台服务器的 Python Worker 监听到该任务，拉起 LangGraph 实例，调用大模型生成 PRD，完成后返回 JSON 给 Temporal。
3. **挂起审核：** Temporal 工作流进入等待状态。用户通过可视化的控制台（可选对接 Camunda 面板或自研 UI）查看 PRD，点击 Approve。
4. **长程作业：** Temporal 收到 Signal，触发 `CodeGenerationActivity`。Python Worker 中的 Coder Agent 开始执行，它会连接 Milvus 检索代码库，分步生成 Diff Patch。
5. **驳回与修复：** 生成完毕后，进入代码评审审批环节。如果评审（人类或 Review Agent）发现 Bug 发起 Reject。Temporal 捕捉异常或特定状态，触发补偿逻辑；LangGraph 侧利用时间旅行回溯图状态，追加报错日志进行重新生成。
6. **交付集成：** 所有阶段通过，系统调用 Git 模块创建 MR，并通知用户。

------

## 四、 架构优势总结

1. **极端的容错性与健壮性：** Spring Boot 结合 Temporal 完美屏蔽了 AI 调用的不确定性（超时、宕机、限流）。即使 Java 调度服务或 Python Worker 重启，流水线也能在断点处精准恢复，这在动辄消耗数十万 Token 的长周期任务中极其关键。
2. **职责边界清晰（强解耦）：** Java 专注于处理高并发调度、事务控制、持久化及对接业务方 API；Python 专注于 Prompt 编排、向量运算及 LLM 交互。异构系统通过 Temporal Server 原生通信，没有冗杂的 HTTP 同步等待。
3. **高度透明的演进过程：** LangGraph 的 State 机制避免了传统 Agent 多轮对话带来的“上下文雪崩”。每一步产物结构化存储，便于前端进行可观测性展示（耗时、Token、中间状态）。
4. **降维打击的交互体验：** 通过 Babel 静态分析与前端注入技术，将 AI 从“聊天框”解放到“所见即所得”的页面实体上，真正实现了“将网页作为操作面板”的终极形态。